var entry = require('./../lib');

console.log('Checking mapped ports to this IP...')
entry.mine(function(err, list){
  if (err)
    return console.log(err);

  console.log('Got ' + list.length + ' mappings for this node.')
  console.log(list);
})
